async function load(){try{const rows=await fetch('/api/admin/students',{cache:'no-store'}).then(r=>r.json());document.getElementById('studentTable').innerHTML=rows.map(s=>`<tr><td>${esc(s.id)}</td><td>${esc(s.name)}</td><td>${s.records||0}</td><td>${s.avg_score??0}%</td><td><a class="button secondary" href="/admin/student/${encodeURIComponent(s.id)}">View details</a></td></tr>`).join('')||'<tr><td colspan="5"><div class="empty">No student records yet.</div></td></tr>'}catch(e){console.error(e)}}load();setInterval(load,5000)
